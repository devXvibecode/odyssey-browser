name: Debug Workflow

on:
  push:
    branches: [ master ]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Echo hello
      run: echo "Hello from GitHub Actions!"
    - name: Check Java
      run: java -version
    - name: Check directory
      run: ls -la